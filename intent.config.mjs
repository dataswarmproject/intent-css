// Intent build for this repository's demo site (docs/ is the GitHub Pages root).
// This file doubles as a reference config — every field has a sensible default.
export default {
  content: ["docs"],
  output: "docs/intent.css",
  brand: { hue: 264, chroma: 0.16 },
};
